export enum Genders {
  MALE = 'male',
  FEMALE = 'female',
}

export enum Roles {
  USER = 'user',
  ADMIN = 'admin',
}

export enum UserType {
  GUEST = 'guest',
  STAFF = 'staff',
  GHOST = 'ghost',
}
